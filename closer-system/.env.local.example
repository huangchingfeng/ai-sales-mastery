# Firebase 設定
# 從 Firebase Console 取得：https://console.firebase.google.com/
# 專案設定 > 一般 > 您的應用程式 > Firebase SDK snippet

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ===== 設定步驟 =====
#
# 1. 建立 Firebase 專案
#    - 前往 https://console.firebase.google.com/
#    - 建立新專案或使用現有專案
#
# 2. 啟用 Authentication
#    - 左側選單 > Authentication > Sign-in method
#    - 啟用「電子郵件/密碼」
#
# 3. 建立 Firestore 資料庫
#    - 左側選單 > Firestore Database
#    - 建立資料庫（選擇生產模式）
#    - 區域選擇 asia-east1（台灣）或 asia-southeast1（新加坡）
#
# 4. 設定安全規則
#    Firestore > 規則，貼上以下內容：
#
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        // 用戶只能讀寫自己的資料
#        match /users/{userId}/{document=**} {
#          allow read, write: if request.auth != null && request.auth.uid == userId;
#        }
#      }
#    }
#
# 5. 取得 Web 應用程式設定
#    - 專案設定 > 一般 > 您的應用程式
#    - 點擊「新增應用程式」> 選擇 Web（</>）
#    - 複製 firebaseConfig 中的值到上方環境變數
#
# 6. 建立 .env.local 檔案
#    cp .env.local.example .env.local
#    然後填入實際的值
